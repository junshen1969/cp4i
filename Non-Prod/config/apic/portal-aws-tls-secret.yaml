apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: portal-aws-secret-manager
  namespace: apic
  annotations:
    argocd.argocd.io/sync-wave: "-4"
spec:
  data:
  - remoteRef:
      conversionStrategy: Default
      decodingStrategy: Base64
      key: certs/apic/api-portal
      property: ca_crt
    secretKey: ca.crt
  - remoteRef:
      conversionStrategy: Default
      decodingStrategy: Base64
      key: certs/apic/api-portal
      property: tls_crt
    secretKey: tls.crt
  - remoteRef:
      conversionStrategy: Default
      decodingStrategy: Base64
      key: certs/apic/api-portal
      property: tls_key
    secretKey: tls.key
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: externalsecretstore
  target:
    creationPolicy: Owner
    deletionPolicy: Retain
    name: portal-tls-secret
